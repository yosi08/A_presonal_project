# 프로덕션 환경변수 파일
# 이 파일은 절대 Git에 올리지 마세요 (.gitignore에 추가할 것)

# PostgreSQL
POSTGRES_USER=occount
POSTGRES_PASSWORD=여기에_강력한_비밀번호_입력  # 예: openssl rand -base64 32
POSTGRES_DB=occount_db

# API 서버
PORT=8080

# JWT (최소 32자 이상의 랜덤 문자열)
JWT_SECRET=여기에_강력한_JWT_시크릿_입력      # 예: openssl rand -base64 48
JWT_EXPIRES_IN=7d
